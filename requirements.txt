# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
jinja2==3.1.5
python-multipart==0.0.19
aiofiles==24.1.0

# Database
sqlalchemy==2.0.36

# HTTP client
httpx==0.28.1

# Scheduling
apscheduler==3.10.4

# Configuration
python-dotenv==1.0.1

# Data processing
pydantic==2.10.4

# Logging
loguru==0.7.3

# Face recognition (Phase 3) is optional because dlib often requires
# native build tooling during deployment. Install requirements-face.txt
# when you want to enable those endpoints locally or in a custom image.
Pillow==11.1.0
numpy>=1.24.0
